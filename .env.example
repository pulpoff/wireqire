# WireGuard QR Manager Configuration
# Copy this to .env and modify for your environment

# =============================================================================
# WIREGUARD SERVER SETTINGS (REQUIRED)
# =============================================================================

# Your WireGuard interface name (usually wg0)
WG_INTERFACE=wg0

# Your server's WireGuard PUBLIC key
# Get it with: sudo cat /etc/wireguard/publickey
# Or: sudo wg show wg0 public-key
WG_SERVER_PUBLIC_KEY=your_server_public_key_here

# Your server's public IP/domain and WireGuard port
# Format: hostname:port or ip:port
WG_SERVER_ENDPOINT=vpn.yourdomain.com:51820

# =============================================================================
# CLIENT SETTINGS
# =============================================================================

# DNS servers for clients (comma-separated)
WG_DNS=1.1.1.1, 1.0.0.1

# What IPs clients can route through VPN
# 0.0.0.0/0, ::/0 = route ALL traffic through VPN
# 10.0.0.0/24 = only route specific subnet
WG_ALLOWED_IPS=0.0.0.0/0, ::/0

# =============================================================================
# IP ALLOCATION
# =============================================================================

# First 3 octets of client IP range
WG_SUBNET=10.10.0

# Starting IP number for clients (e.g., 10 means first client gets .10)
WG_START_IP=10

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

# Database location
DATABASE_URL=sqlite:///./wireguard_peers.db

# API bind address and port
APP_HOST=0.0.0.0
APP_PORT=6000
